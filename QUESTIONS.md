# QUESTIONS — batched for Lillian

Appended by implementer/coordinator sessions during autonomous runs (`/run-phase`). The coordinator surfaces open items at every hard-stop checkpoint (immediately, if blocking). When Lillian answers, the item moves to **Answered** with the resolution and where it landed; any PROVISIONAL implementation it references flips to final (or gets reworked) at that point.

Format per item:

- **[phase · date]** The question — options considered → what was provisionally implemented (file / phase-entry reference).

## Open

*(none — fully cleared at the 2026-07-11 product session)*

## Answered

- **[N3 · 2026-07-06 → answered 2026-07-11]** The five N3 PROVISIONALs — `.mkv` provenance (`sources.original_path` column) / `.fixedPadding` tail clamping (clamp to next clip's first word AND source duration) / re-apply semantics (ID-preserving diff, tags survive unchanged ranges) / ingest undo granularity (one grouped "Ingest Folder" step) / failed-remux disposition (hard reject, `remux-failed`, re-ingest picks it up) — **Answered (Lillian, 2026-07-11, product session): ALL FIVE keep as implemented.** The PHASES.md N3 PROVISIONALs are final. Session record: `PRODUCT_SESSION_2026-07-11.md`.
- **[Product session · 2026-07-11]** Ten further design/product calls resolved the same session: **D35** generation timing (on-demand offer + per-source action + Settings auto-toggle default OFF; cut math/frame nudges/keyframe ticks ALWAYS from the original, never the proxy), **D36** trim-mode edit-point semantics (incoming frame parked on a cut; per-burst nudge undo), **D37** assembly outline shape A + Duplicate Project (Track 1 — N6/N7/N8 amendments), **D38** no multi-machine sync (on record), **D39** N12 staged-split pre-authorization (N12a Standard-first contingency), **D40** Track 1.5 (N13.5 quality iteration with early-pull trigger + audio-energy promotion from N19; N13.6 ask-the-library). Landed in `NATIVE_REWRITE_DECISIONS.md`, `NATIVE_REWRITE_PLAN.md`, spec amendment 15; session record `PRODUCT_SESSION_2026-07-11.md`.
- **[N3 cold-review residual · 2026-07-06 → answered 2026-07-11]** Sibling-`.mp4` adoption warning coverage boundary — `x.mkv` arriving after `x.mp4` was ingested in an earlier run is skipped with no reason given. Options: (a) leave as-is / (b) warn specifically / (c) ingest under a disambiguated name. **Answered (Lillian, 2026-07-11, product session): (b) — specific warning** ("video.mkv skipped — video.mp4 is already in your library; if this is different content, rename and re-ingest"). Auto-disambiguation rejected (the mkv is usually the same video in a different container — would silently double the clips). **Owner: N4** (added to the N4 kickoff + plan entry).
- **[N2 · 2026-07-06]** Synthetic fixtures for codec variety (PROVISIONAL 1) — synthesize deterministic AVAssetWriter fixtures / block on real files from Lillian / skip those gate legs → implemented **synthetic fixtures**, real files used wherever they qualify, per-report material labels. **Answered (Lillian, 2026-07-06, via coordinator question round): keep as implemented.** Standing pattern for future phases; the HDR row's fixture-encode caveat is exactly why it defers to her real iPhone HDR clip.
- **[N2 · 2026-07-06]** Gate instrumentation (PROVISIONAL 2) — programmatic decode-level measurement with perceptual confirmation at the HARD-STOP watch session / defer all measurement to Lillian → implemented **programmatic + watch-session confirmation**. **Answered (Lillian, 2026-07-06): keep as implemented.** Post-black-window addendum stands: presentation-layer self-checks print in stdout on every demo run so machine-verifiable display state rides alongside her eyeball.
- **[N2 · 2026-07-06]** Debug-harness placement (PROVISIONAL 3) — SPM executable target in ClipFarmKit / separate package / app-target debug screen → implemented the **SPM executable target** (`n2harness`, never ships). **Answered (Lillian, 2026-07-06): keep as implemented.** Future phase harnesses follow the same pattern.
- **[N2 adjudication · 2026-07-06]** Proxy-workflow phase owner — N12 export / N3 ingest background job / dedicated phase → implemented **N12 as owner, PROVISIONAL**; D35 drafted PROPOSED. **Answered (Lillian, 2026-07-06): D35 RATIFIED — LOCKED as drafted.** N12 ownership confirmed (proxy *generation* timing still decided when N12 lands, per the decision text); §6 budgets stay as the bar the proxy path must meet; D11 pivot rejected. Decision-log status flipped.

- **[N3 prep · 2026-07-06]** swift-subprocess as N3's first new third-party dependency — researched per Lillian's instruction (single Opus web-research agent; full vetting report delivered in-session 2026-07-06): Apple/swiftlang-governed, Apache-2.0 (clean for closed-source paid distribution, explicit patent grant), `1.0.0-beta.1` tagged 2026-07-02 after a breaking 0.5 redesign; pulls in swift-system (also swiftlang/Apache-2.0); alternative was a ~50-line Foundation.Process wrapper (adequate for remux-only, no output capture needed). **Answered (Lillian, 2026-07-06): APPROVED — adopt, pinned exactly to `1.0.0-beta.1`** (`.exact`, no floating range), behind the locator seam per plan; bump to the 1.0 GA tag as a deliberate reviewed step when it ships; re-commit **both** `Package.resolved` lockfiles per the GRDB convention. The N3 kickoff's approval gate is satisfied — N3 may land the dependency without further ask.

- **[iCloud incident · 2026-07-06]** Footage folder still on iCloud-synced Desktop after the repo's emergency move — leave / move / exclude from sync? **Answered (Lillian, 2026-07-06): dedicated footage inbox — D34 (LOCKED), spec amendment 14.** ClipFarm reads from a drop folder outside any cloud-synced path, default `~/ClipFarm/Footage/` (folder created). The inbox is a **managed working folder, not canonical storage** — ClipFarm may organize and delete files within it ("every existing video editor lets you delete the clips you drop in"); the strictly-read-only rule now applies only to footage *outside* the inbox. The old `~/Desktop/AdAstra/…` path is retired from operative docs — existing files stay put; Lillian moves files into the inbox herself when ready to test. Landed in: spec amendment 14, D34, `mac/CLAUDE.md` (Lillian-only bullet), N2 kickoff + PHASES.md N2 entry, plan N3 scope/verify.
- **[N1 · 2026-07-06]** Are per-library **settings writes undoable**? mac/CLAUDE.md's blanket "every store mutation lands with a register→undo→redo test" vs the macOS convention that config changes never sit on the document undo stack — options: (a) undo-register settings writes too / (b) scope the undo rule to library-*content* mutations / (c) defer settings entirely to N3 → implemented **(b)**. **Answered (Lillian, 2026-07-06): keep as implemented — content-only undo; settings writes are not undoable.** Final in `mac/ClipFarmKit/Sources/CFStore/LibrarySettings.swift`; COMPLETED_PHASES.md → N1 provisional 1 flipped.
- **[N1 · 2026-07-06]** Snapshot filename token: the reference used a 4-char **content hash** to avoid same-millisecond collisions; with `VACUUM INTO` the content isn't knowable before the copy — options: hash the live DB file bytes (reads whole file + WAL) / random 4-hex token (same collision-avoidance purpose, no content claim) / monotonic counter in `meta` → implemented the **random token**. **Answered (Lillian, 2026-07-06): keep as implemented.** Final in `Snapshots.swift`; N1 provisional 2 flipped.
- **[N1 · 2026-07-06]** `tailPaddingSec` default for D18's fixed-padding tail policy ("+N ms", no N specified) — options: 0.0 (inert until N3's segmentation UI exposes it) / invent ~0.25s / no default → implemented **0.0**. **Answered (Lillian, 2026-07-06): CHANGED to 0.25s** — reworked in `LibrarySettings.swift` (+ tests); N3's segmentation UI can still revisit once results are audible. N1 provisional 3 reworked.
- **[N0 review · 2026-07-06]** `.obsidian/workspace.json` is tracked in git and keeps dirtying the working tree (cold-reviewer observation, out of N0 scope). Gitignore `.obsidian/` (+ `git rm --cached` the tracked file)? **Answered (Lillian, 2026-07-06): AUTHORIZED.** `.obsidian/` added to the root `.gitignore`; all four tracked files under it untracked via `git rm --cached` (workspace.json was the named offender, but a directory-level ignore with siblings still tracked would keep recreating the dirty-tree problem — flagged to the coordinator). Files remain on disk untouched.
- **[N0 · 2026-07-05]** GRDB "pinned" mechanics — `exact: "7.x.y"` in Package.swift vs `from: "7.0.0"` (major-locked) with the exact version pinned by the committed `Package.resolved` vs vendoring → implemented `from: "7.0.0"` + committed `Package.resolved`, resolved at 7.11.1. **Answered (Lillian, 2026-07-05): keep as implemented — no `exact:` pin.** Landed as final in `mac/ClipFarmKit/Package.swift`; COMPLETED_PHASES.md → N0 flipped. Cold-review addendum: two lockfiles exist (Kit + xcodeproj workspace) — after any dependency re-resolve, re-commit **both** (rule recorded in `mac/CLAUDE.md` → Commands).
- **[N0 · 2026-07-05]** Package product shape — one umbrella `ClipFarmKit` library product exporting all five targets vs five separate products → implemented the umbrella product (one product dependency in the pbxproj; the app still imports modules individually). **Answered (Lillian, 2026-07-05): keep as implemented.** Final in `mac/ClipFarmKit/Package.swift`.
- **[N0 · 2026-07-05]** Intra-Kit target dependency graph — plan §2.2 fixes CFStore→GRDB and CFDomain→nothing but doesn't draw the rest (e.g. CFExport→CFMedia) → implemented the minimal graph: every non-domain target depends only on CFDomain (+ GRDB for CFStore). **Answered (Lillian, 2026-07-05): keep as implemented — grow edges as phases need them.** Final in `mac/ClipFarmKit/Package.swift`.
- **[N0 · 2026-07-05]** Swift language mode on the app target — `SWIFT_VERSION=5.0` (Apple's migration-friendly default) vs `6.0` (strict concurrency, symmetric with Kit targets under swift-tools 6.2) → implemented 6.0. **Answered (Lillian, 2026-07-05): keep as implemented.** Final in `mac/ClipFarm.xcodeproj/project.pbxproj`.
