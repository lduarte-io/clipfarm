export default function Settings() {
  return (
    <section>
      <h1 className="text-2xl font-semibold mb-2">Settings</h1>
      <p className="text-neutral-400">
        Not yet implemented. Transcript folder, Ollama endpoint, model choice,
        export defaults land alongside the features that need them.
      </p>
    </section>
  );
}
