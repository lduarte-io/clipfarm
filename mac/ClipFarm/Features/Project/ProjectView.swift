import SwiftUI

/// Project page placeholder — take grid by line (N8).
struct ProjectView: View {
    var body: some View {
        ContentUnavailableView("Project", systemImage: "square.grid.2x2")
    }
}
