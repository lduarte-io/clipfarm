import SwiftUI

/// Settings page placeholder — provider, models, export defaults, library
/// location (N7 onward).
struct SettingsView: View {
    var body: some View {
        ContentUnavailableView("Settings", systemImage: "gearshape")
    }
}
