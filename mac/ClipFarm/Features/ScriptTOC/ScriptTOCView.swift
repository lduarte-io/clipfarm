import SwiftUI

/// Script TOC page placeholder — reorderable outline, takes inline (N8).
struct ScriptTOCView: View {
    var body: some View {
        ContentUnavailableView("Script", systemImage: "list.number")
    }
}
