import SwiftUI

/// Brief editor page placeholder — script, TOC, "what's good", tags (N6).
struct BriefView: View {
    var body: some View {
        ContentUnavailableView("Brief", systemImage: "doc.text")
    }
}
