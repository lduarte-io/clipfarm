import SwiftUI

/// Attempts page placeholder — two premade buckets + attempt detail (N9/N10).
struct AttemptsView: View {
    var body: some View {
        ContentUnavailableView("Attempts", systemImage: "film.stack")
    }
}
