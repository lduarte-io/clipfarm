import SwiftUI

/// Library page placeholder — sources, clips, transcript browser (N4).
struct LibraryView: View {
    var body: some View {
        ContentUnavailableView("Library", systemImage: "books.vertical")
    }
}
