/// CFLLM — Ollama + Anthropic clients and the tagging/naming orchestrators.
/// Provider choice never leaks past the dispatcher in this module.
///
/// N0 placeholder. Clients + orchestrators land at N7.
public enum CFLLMModule {
    public static let name = "CFLLM"
}
