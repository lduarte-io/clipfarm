/// CFDomain — pure domain logic: value types + pure functions, zero dependencies, no I/O.
///
/// N0 placeholder. Domain rules (segmentation, propagation, trim resolution,
/// continuity, strategies) land here from N1 onward.
public enum CFDomainModule {
    public static let name = "CFDomain"
}
