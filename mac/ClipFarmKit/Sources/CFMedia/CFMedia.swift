/// CFMedia — probe, composition, PlayerEngine, thumbnails, waveforms, keyframe
/// maps. AVFoundation lives here and only here.
///
/// N0 placeholder. AssetCache / MetadataProbe / CompositionBuilder / PlayerEngine
/// land at N2.
public enum CFMediaModule {
    public static let name = "CFMedia"
}
