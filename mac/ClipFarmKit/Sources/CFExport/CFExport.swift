/// CFExport — export tiers, hybrid writer, ffmpeg (.mkv remux) behind the
/// FFmpegLocator seam.
///
/// N0 placeholder. Export engine lands at N12; remux path at N3.
public enum CFExportModule {
    public static let name = "CFExport"
}
